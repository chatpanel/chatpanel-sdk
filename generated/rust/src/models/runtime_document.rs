/*
 * ChatPanel Gateway API
 *
 * The ChatPanel gateway runs on the user's machine (loopback only, port 4320 by default). It redacts personal data before a model sees it, routes model traffic, and holds the user's searchable history, durable memory, shared preferences and the team/project run boards.  **It only answers on loopback.** A client on another host cannot reach it; an SDK refuses a non-loopback base URL unless the integrator opts in and uses TLS.  **Two trust levels.** The `/v1` data plane is open to any local process — that is the product. Writes that change what every client sees (remember, forget, ingest) and anything admin-shaped require the per-install token the gateway writes to `~/.chatpanel/gateway-token` (mode 0600), or a copy obtained by pairing (`POST /pair` with a one-time code from `chatpanel-gateway pair`). `POST /whoami` says which trust you have.  **Errors** are `{ error: { message, type } }` (or `{ error: \"text\" }` on the oldest routes) with the HTTP status. `type` is a stable machine word: `not_found`, `invalid_request`, `unknown_endpoint`, `unavailable`, `bridge_unreachable`, `no_bridge`, `redact_error`, `search_error`, `memory_error`, `team_error`, `ingest_error`, `prefs_error`. 
 *
 * The version of the OpenAPI document: 0.22.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// RuntimeDocument : The runtime — Settings › Runtime's one document (docs/sandboxing.md S1).
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct RuntimeDocument {
    /// the bridge's /health.sandbox: enabled, mode, runtime, reason?, extras, ownSessions, refused[] (names only), provisioned? (Windows)
    #[serde(rename = "sandbox", skip_serializing_if = "Option::is_none")]
    pub sandbox: Option<std::collections::HashMap<String, serde_json::Value>>,
    #[serde(rename = "processes", skip_serializing_if = "Option::is_none")]
    pub processes: Option<Box<models::RuntimeDocumentProcesses>>,
    /// every container the engine has, running or not: name, image, state, status, ports[], ours (a chatpanel- name), engine (0.19.1)
    #[serde(rename = "containers", skip_serializing_if = "Option::is_none")]
    pub containers: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    /// { id, engine, host, at } newest first
    #[serde(rename = "refused", skip_serializing_if = "Option::is_none")]
    pub refused: Option<Vec<std::collections::HashMap<String, serde_json::Value>>>,
    #[serde(rename = "bridge", skip_serializing_if = "Option::is_none")]
    pub bridge: Option<Box<models::RuntimeDocumentBridge>>,
    /// podman/docker: { installed, version?, running?, machine? }; preferred; install? { command, url, note }
    #[serde(rename = "engines", skip_serializing_if = "Option::is_none")]
    pub engines: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// Per catalogue service (searxng · reranker · opendecision): { id, label, image, container, port, blurb, provides, engine, state, url, configured, answering } — a capability container also { model, default, models: [{ id, label, lang, tier, approxMB, ramMB, licence, recommended, installed, note, unavailable?, ramNote? }], machine: { engineRamMB } } (gateway 0.22+).
    #[serde(rename = "services", skip_serializing_if = "Option::is_none")]
    pub services: Option<std::collections::HashMap<String, serde_json::Value>>,
}

impl RuntimeDocument {
    /// The runtime — Settings › Runtime's one document (docs/sandboxing.md S1).
    pub fn new() -> RuntimeDocument {
        RuntimeDocument {
            sandbox: None,
            processes: None,
            containers: None,
            refused: None,
            bridge: None,
            engines: None,
            services: None,
        }
    }
}

