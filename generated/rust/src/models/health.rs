/*
 * ChatPanel Gateway API
 *
 * The ChatPanel gateway runs on the user's machine (loopback only, port 4320 by default). It redacts personal data before a model sees it, routes model traffic, and holds the user's searchable history, durable memory, shared preferences and the team/project run boards.  **It only answers on loopback.** A client on another host cannot reach it; an SDK refuses a non-loopback base URL unless the integrator opts in and uses TLS.  **Two trust levels.** The `/v1` data plane is open to any local process — that is the product. Writes that change what every client sees (remember, forget, ingest) and anything admin-shaped require the per-install token the gateway writes to `~/.chatpanel/gateway-token` (mode 0600), or a copy obtained by pairing (`POST /pair` with a one-time code from `chatpanel-gateway pair`). `POST /whoami` says which trust you have.  **Errors** are `{ error: { message, type } }` (or `{ error: \"text\" }` on the oldest routes) with the HTTP status. `type` is a stable machine word: `not_found`, `invalid_request`, `unknown_endpoint`, `unavailable`, `bridge_unreachable`, `no_bridge`, `redact_error`, `search_error`, `memory_error`, `team_error`, `ingest_error`, `prefs_error`. 
 *
 * The version of the OpenAPI document: 0.15.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct Health {
    #[serde(rename = "ok")]
    pub ok: bool,
    /// The gateway's semver; every version gate reads this.
    #[serde(rename = "version")]
    pub version: String,
    #[serde(rename = "backend", skip_serializing_if = "Option::is_none")]
    pub backend: Option<String>,
    /// Redaction tier.
    #[serde(rename = "tier", skip_serializing_if = "Option::is_none")]
    pub tier: Option<Tier>,
    /// Present from 0.9.0 — this gateway can pair a client.
    #[serde(rename = "pairing", skip_serializing_if = "Option::is_none")]
    pub pairing: Option<bool>,
    /// A domain object whose shape is owned by `@chatpanel/events`; the SDK carries it as-is.
    #[serde(rename = "managed", skip_serializing_if = "Option::is_none")]
    pub managed: Option<std::collections::HashMap<String, serde_json::Value>>,
    #[serde(rename = "managedBy", skip_serializing_if = "Option::is_none")]
    pub managed_by: Option<String>,
    /// A domain object whose shape is owned by `@chatpanel/events`; the SDK carries it as-is.
    #[serde(rename = "bridge", skip_serializing_if = "Option::is_none")]
    pub bridge: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// A domain object whose shape is owned by `@chatpanel/events`; the SDK carries it as-is.
    #[serde(rename = "stt", skip_serializing_if = "Option::is_none")]
    pub stt: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// A domain object whose shape is owned by `@chatpanel/events`; the SDK carries it as-is.
    #[serde(rename = "tts", skip_serializing_if = "Option::is_none")]
    pub tts: Option<std::collections::HashMap<String, serde_json::Value>>,
}

impl Health {
    pub fn new(ok: bool, version: String) -> Health {
        Health {
            ok,
            version,
            backend: None,
            tier: None,
            pairing: None,
            managed: None,
            managed_by: None,
            bridge: None,
            stt: None,
            tts: None,
        }
    }
}
/// Redaction tier.
#[derive(Clone, Copy, Debug, Eq, PartialEq, Ord, PartialOrd, Hash, Serialize, Deserialize)]
pub enum Tier {
    #[serde(rename = "basic")]
    Basic,
    #[serde(rename = "full")]
    Full,
}

impl Default for Tier {
    fn default() -> Tier {
        Self::Basic
    }
}

