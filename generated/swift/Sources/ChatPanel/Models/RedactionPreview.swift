//
// RedactionPreview.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct RedactionPreview: Sendable, Codable, Hashable {

    public enum Tier: String, Sendable, Codable, CaseIterable {
        case basic = "basic"
        case full = "full"
    }
    /** What the model would receive. */
    public var text: String
    public var count: Int
    public var sanitized: Int?
    public var tier: Tier?
    /** Placeholder tokens and their types — never the real values. */
    public var entities: [Dictionary]?
    /** A domain object whose shape is owned by `@chatpanel/events`; the SDK carries it as-is. */
    public var detector: [String: JSONValue]?

    public init(text: String, count: Int, sanitized: Int? = nil, tier: Tier? = nil, entities: [Dictionary]? = nil, detector: [String: JSONValue]? = nil) {
        self.text = text
        self.count = count
        self.sanitized = sanitized
        self.tier = tier
        self.entities = entities
        self.detector = detector
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case text
        case count
        case sanitized
        case tier
        case entities
        case detector
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(text, forKey: .text)
        try container.encode(count, forKey: .count)
        try container.encodeIfPresent(sanitized, forKey: .sanitized)
        try container.encodeIfPresent(tier, forKey: .tier)
        try container.encodeIfPresent(entities, forKey: .entities)
        try container.encodeIfPresent(detector, forKey: .detector)
    }
}

