//
// A2AResult.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** The reply, plus the two facts every caller derives — done, and what a person must do. */
public struct A2AResult: Sendable, Codable, Hashable {

    public enum Kind: String, Sendable, Codable, CaseIterable {
        case task = "task"
        case message = "message"
    }
    public enum Needs: String, Sendable, Codable, CaseIterable {
        case answer = "answer"
        case approval = "approval"
    }
    public var ok: Bool?
    /** A2A returns one or the other; an agent answering at once creates no task. */
    public var kind: Kind?
    public var task: [String: JSONValue]?
    public var message: [String: JSONValue]?
    /** The answer as text — artifacts first, then what the agent actually said. */
    public var text: String?
    public var done: Bool?
    /** `answer` for TASK_STATE_INPUT_REQUIRED, `approval` for TASK_STATE_AUTH_REQUIRED, null otherwise. */
    public var needs: Needs?

    public init(ok: Bool? = nil, kind: Kind? = nil, task: [String: JSONValue]? = nil, message: [String: JSONValue]? = nil, text: String? = nil, done: Bool? = nil, needs: Needs? = nil) {
        self.ok = ok
        self.kind = kind
        self.task = task
        self.message = message
        self.text = text
        self.done = done
        self.needs = needs
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case ok
        case kind
        case task
        case message
        case text
        case done
        case needs
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(ok, forKey: .ok)
        try container.encodeIfPresent(kind, forKey: .kind)
        try container.encodeIfPresent(task, forKey: .task)
        try container.encodeIfPresent(message, forKey: .message)
        try container.encodeIfPresent(text, forKey: .text)
        try container.encodeIfPresent(done, forKey: .done)
        try container.encodeIfPresent(needs, forKey: .needs)
    }
}

