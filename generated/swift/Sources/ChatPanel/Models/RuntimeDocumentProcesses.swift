//
// RuntimeDocumentProcesses.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct RuntimeDocumentProcesses: Sendable, Codable, Hashable {

    /** every process running for the user — the bridge's (kind agent | warm | mcp | probe) and the gateway's own workers (kind worker): id, kind, engine, command (basename), label, pid, since, sandbox (the record), refused[] — never argv or env (0.19.1) */
    public var all: [Dictionary]?
    /** id, command (basename), pid, since, sandbox (the record), refused[] — never argv or env */
    public var localMcp: [Dictionary]?
    /** A domain object whose shape is owned by `@chatpanel/events`; the SDK carries it as-is. */
    public var warm: [String: JSONValue]?

    public init(all: [Dictionary]? = nil, localMcp: [Dictionary]? = nil, warm: [String: JSONValue]? = nil) {
        self.all = all
        self.localMcp = localMcp
        self.warm = warm
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case all
        case localMcp
        case warm
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(all, forKey: .all)
        try container.encodeIfPresent(localMcp, forKey: .localMcp)
        try container.encodeIfPresent(warm, forKey: .warm)
    }
}

