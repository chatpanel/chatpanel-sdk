//
// ReadRestriction.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct ReadRestriction: Sendable, Codable, Hashable {

    public enum Reason: String, Sendable, Codable, CaseIterable {
        case login = "login"
        case paywall = "paywall"
        case rateLimited = "rate_limited"
        case robots = "robots"
        case tdm = "tdm"
    }
    /** `robots` and `tdm` come only from a hosted (crawler) provider; on the user's machine the reader is a user agent. */
    public var reason: Reason
    public var detail: String?

    public init(reason: Reason, detail: String? = nil) {
        self.reason = reason
        self.detail = detail
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case reason
        case detail
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(reason, forKey: .reason)
        try container.encodeIfPresent(detail, forKey: .detail)
    }
}

