//
// Health.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct Health: Sendable, Codable, Hashable {

    public enum Tier: String, Sendable, Codable, CaseIterable {
        case basic = "basic"
        case full = "full"
    }
    public var ok: Bool
    /** The gateway's semver; every version gate reads this. */
    public var version: String
    public var backend: String?
    /** Redaction tier. */
    public var tier: Tier?
    /** Present from 0.9.0 — this gateway can pair a client. */
    public var pairing: Bool?
    /** A domain object whose shape is owned by `@chatpanel/events`; the SDK carries it as-is. */
    public var managed: [String: JSONValue]?
    public var managedBy: String?
    /** A domain object whose shape is owned by `@chatpanel/events`; the SDK carries it as-is. */
    public var bridge: [String: JSONValue]?
    /** A domain object whose shape is owned by `@chatpanel/events`; the SDK carries it as-is. */
    public var stt: [String: JSONValue]?
    /** A domain object whose shape is owned by `@chatpanel/events`; the SDK carries it as-is. */
    public var tts: [String: JSONValue]?

    public init(ok: Bool, version: String, backend: String? = nil, tier: Tier? = nil, pairing: Bool? = nil, managed: [String: JSONValue]? = nil, managedBy: String? = nil, bridge: [String: JSONValue]? = nil, stt: [String: JSONValue]? = nil, tts: [String: JSONValue]? = nil) {
        self.ok = ok
        self.version = version
        self.backend = backend
        self.tier = tier
        self.pairing = pairing
        self.managed = managed
        self.managedBy = managedBy
        self.bridge = bridge
        self.stt = stt
        self.tts = tts
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case ok
        case version
        case backend
        case tier
        case pairing
        case managed
        case managedBy
        case bridge
        case stt
        case tts
    }

    public var additionalProperties: [String: JSONValue] = [:]

    public subscript(key: String) -> JSONValue? {
        get {
            if let value = additionalProperties[key] {
                return value
            }
            return nil
        }

        set {
            additionalProperties[key] = newValue
        }
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(ok, forKey: .ok)
        try container.encode(version, forKey: .version)
        try container.encodeIfPresent(backend, forKey: .backend)
        try container.encodeIfPresent(tier, forKey: .tier)
        try container.encodeIfPresent(pairing, forKey: .pairing)
        try container.encodeIfPresent(managed, forKey: .managed)
        try container.encodeIfPresent(managedBy, forKey: .managedBy)
        try container.encodeIfPresent(bridge, forKey: .bridge)
        try container.encodeIfPresent(stt, forKey: .stt)
        try container.encodeIfPresent(tts, forKey: .tts)
        var additionalPropertiesContainer = encoder.container(keyedBy: String.self)
        try additionalPropertiesContainer.encodeMap(additionalProperties)
    }

    // Decodable protocol methods

    public init(from decoder: Decoder) throws {
        let container = try decoder.container(keyedBy: CodingKeys.self)

        ok = try container.decode(Bool.self, forKey: .ok)
        version = try container.decode(String.self, forKey: .version)
        backend = try container.decodeIfPresent(String.self, forKey: .backend)
        tier = try container.decodeIfPresent(Tier.self, forKey: .tier)
        pairing = try container.decodeIfPresent(Bool.self, forKey: .pairing)
        managed = try container.decodeIfPresent([String: JSONValue].self, forKey: .managed)
        managedBy = try container.decodeIfPresent(String.self, forKey: .managedBy)
        bridge = try container.decodeIfPresent([String: JSONValue].self, forKey: .bridge)
        stt = try container.decodeIfPresent([String: JSONValue].self, forKey: .stt)
        tts = try container.decodeIfPresent([String: JSONValue].self, forKey: .tts)
        var nonAdditionalPropertyKeys = Set<String>()
        nonAdditionalPropertyKeys.insert("ok")
        nonAdditionalPropertyKeys.insert("version")
        nonAdditionalPropertyKeys.insert("backend")
        nonAdditionalPropertyKeys.insert("tier")
        nonAdditionalPropertyKeys.insert("pairing")
        nonAdditionalPropertyKeys.insert("managed")
        nonAdditionalPropertyKeys.insert("managedBy")
        nonAdditionalPropertyKeys.insert("bridge")
        nonAdditionalPropertyKeys.insert("stt")
        nonAdditionalPropertyKeys.insert("tts")
        let additionalPropertiesContainer = try decoder.container(keyedBy: String.self)
        additionalProperties = try additionalPropertiesContainer.decodeMap(JSONValue.self, excludedKeys: nonAdditionalPropertyKeys)
    }
}

