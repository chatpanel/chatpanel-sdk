//
// PushEventsResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct PushEventsResponse: Sendable, Codable, Hashable {

    public var ok: Bool
    public var appended: Int
    /** Events already held — a retry's share. */
    public var duplicates: Int
    public var rejected: [PushEventsResponseRejectedInner]
    /** `{ host: seq }` — the highest seq held per host. */
    public var cursor: [String: Int]

    public init(ok: Bool, appended: Int, duplicates: Int, rejected: [PushEventsResponseRejectedInner], cursor: [String: Int]) {
        self.ok = ok
        self.appended = appended
        self.duplicates = duplicates
        self.rejected = rejected
        self.cursor = cursor
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case ok
        case appended
        case duplicates
        case rejected
        case cursor
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(ok, forKey: .ok)
        try container.encode(appended, forKey: .appended)
        try container.encode(duplicates, forKey: .duplicates)
        try container.encode(rejected, forKey: .rejected)
        try container.encode(cursor, forKey: .cursor)
    }
}

