//
// RuntimeDocument.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** The runtime — Settings › Runtime&#39;s one document (docs/sandboxing.md S1). */
public struct RuntimeDocument: Sendable, Codable, Hashable {

    /** the bridge's /health.sandbox: enabled, mode, runtime, reason?, extras, ownSessions, refused[] (names only), provisioned? (Windows) */
    public var sandbox: [String: JSONValue]?
    public var processes: RuntimeDocumentProcesses?
    /** every container the engine has, running or not: name, image, state, status, ports[], ours (a chatpanel- name), engine (0.19.1) */
    public var containers: [Dictionary]?
    /** { id, engine, host, at } newest first */
    public var refused: [Dictionary]?
    public var bridge: RuntimeDocumentBridge?
    /** podman/docker: { installed, version?, running?, machine? }; preferred; install? { command, url, note } */
    public var engines: [String: JSONValue]?
    /** Per catalogue service (searxng · reranker · opendecision): { id, label, image, container, port, blurb, provides, engine, state, url, configured, answering } — a capability container also { model, default, models: [{ id, label, lang, tier, approxMB, ramMB, licence, recommended, installed, note, unavailable?, ramNote? }], machine: { engineRamMB } } (gateway 0.22+). */
    public var services: [String: JSONValue]?

    public init(sandbox: [String: JSONValue]? = nil, processes: RuntimeDocumentProcesses? = nil, containers: [Dictionary]? = nil, refused: [Dictionary]? = nil, bridge: RuntimeDocumentBridge? = nil, engines: [String: JSONValue]? = nil, services: [String: JSONValue]? = nil) {
        self.sandbox = sandbox
        self.processes = processes
        self.containers = containers
        self.refused = refused
        self.bridge = bridge
        self.engines = engines
        self.services = services
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case sandbox
        case processes
        case containers
        case refused
        case bridge
        case engines
        case services
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(sandbox, forKey: .sandbox)
        try container.encodeIfPresent(processes, forKey: .processes)
        try container.encodeIfPresent(containers, forKey: .containers)
        try container.encodeIfPresent(refused, forKey: .refused)
        try container.encodeIfPresent(bridge, forKey: .bridge)
        try container.encodeIfPresent(engines, forKey: .engines)
        try container.encodeIfPresent(services, forKey: .services)
    }
}

