//
// RuntimeAPI.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

open class RuntimeAPI {

    /**
     * enum for parameter name
     */
    public enum Name_runtimeEngine: String, Sendable, CaseIterable {
        case podman = "podman"
        case docker = "docker"
    }

    /**
     Start the container engine (Podman — creates and starts its machine where one is needed).
     
     - parameter name: (path)  
     - parameter runtimeEngineRequest: (body)  (optional)
     - parameter apiConfiguration: The configuration for the http request.
     - returns: RuntimeActionResult
     */
    open class func runtimeEngine(name: Name_runtimeEngine, runtimeEngineRequest: RuntimeEngineRequest? = nil, apiConfiguration: ChatPanelAPIConfiguration = ChatPanelAPIConfiguration.shared) async throws(ErrorResponse) -> RuntimeActionResult {
        return try await runtimeEngineWithRequestBuilder(name: name, runtimeEngineRequest: runtimeEngineRequest, apiConfiguration: apiConfiguration).execute().body
    }

    /**
     Start the container engine (Podman — creates and starts its machine where one is needed).
     - POST /v1/runtime/engines/{name}
     - `{ action: 'start' }`. Podman on macOS and Windows runs containers in a machine: made on first start (`podman machine init`), then started. Linux Podman is rootless and needs nothing. Docker is not started by the gateway — the response says so. 
     - API Key:
       - type: apiKey X-ChatPanel-Token (HEADER)
       - name: tokenHeader
     - Bearer Token:
       - type: http
       - name: gatewayToken
     - parameter name: (path)  
     - parameter runtimeEngineRequest: (body)  (optional)
     - parameter apiConfiguration: The configuration for the http request.
     - returns: RequestBuilder<RuntimeActionResult> 
     */
    open class func runtimeEngineWithRequestBuilder(name: Name_runtimeEngine, runtimeEngineRequest: RuntimeEngineRequest? = nil, apiConfiguration: ChatPanelAPIConfiguration = ChatPanelAPIConfiguration.shared) -> RequestBuilder<RuntimeActionResult> {
        var localVariablePath = "/v1/runtime/engines/{name}"
        let namePreEscape = "\(name.rawValue)"
        let namePostEscape = namePreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{name}", with: namePostEscape, options: .literal, range: nil)
        let localVariableURLString = apiConfiguration.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: runtimeEngineRequest, codableHelper: apiConfiguration.codableHelper)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: (any Sendable)?] = [
            "Content-Type": "application/json",
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<RuntimeActionResult>.Type = apiConfiguration.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true, apiConfiguration: apiConfiguration)
    }

    /**
     * enum for parameter id
     */
    public enum Id_runtimeService: String, Sendable, CaseIterable {
        case searxng = "searxng"
        case reranker = "reranker"
        case opendecision = "opendecision"
    }

    /**
     Start or stop a catalogue service, or pick a capability container's model — each runs loopback-only and the gateway points at it.
     
     - parameter id: (path)  
     - parameter runtimeServiceRequest: (body)  (optional)
     - parameter apiConfiguration: The configuration for the http request.
     - returns: RuntimeActionResult
     */
    open class func runtimeService(id: Id_runtimeService, runtimeServiceRequest: RuntimeServiceRequest? = nil, apiConfiguration: ChatPanelAPIConfiguration = ChatPanelAPIConfiguration.shared) async throws(ErrorResponse) -> RuntimeActionResult {
        return try await runtimeServiceWithRequestBuilder(id: id, runtimeServiceRequest: runtimeServiceRequest, apiConfiguration: apiConfiguration).execute().body
    }

    /**
     Start or stop a catalogue service, or pick a capability container's model — each runs loopback-only and the gateway points at it.
     - POST /v1/runtime/services/{id}
     - `{ action: 'start' | 'stop' }`. `start` brings the engine up if it is not, runs the service's container from its kit (SearXNG: `127.0.0.1:8888`, JSON on, the limiter off, a random secret, capabilities dropped; `reranker` on 8889 and `opendecision` on 8890 with the shared model cache mounted, gateway 0.20+) — the first start pulls the image — waits for it to answer, and sets the config that points the gateway at it (`search.searxng.url`; `capabilities.rerank` / `capabilities.decide`, so `/v1/rerank` and `/v1/decide` are served and listed). `stop` stops the container and clears what it set. `{ action: 'model', model }` (gateway 0.22+) picks the model a capability container runs — an id from its catalogue (`GET /v1/runtime` `services.<id>.models`) or a Hugging Face `owner/name` — gated by the container ENGINE's memory (a model it cannot hold is refused with the command that raises the ceiling); the container is re-created with the pick and restarted when it ran. 
     - API Key:
       - type: apiKey X-ChatPanel-Token (HEADER)
       - name: tokenHeader
     - Bearer Token:
       - type: http
       - name: gatewayToken
     - parameter id: (path)  
     - parameter runtimeServiceRequest: (body)  (optional)
     - parameter apiConfiguration: The configuration for the http request.
     - returns: RequestBuilder<RuntimeActionResult> 
     */
    open class func runtimeServiceWithRequestBuilder(id: Id_runtimeService, runtimeServiceRequest: RuntimeServiceRequest? = nil, apiConfiguration: ChatPanelAPIConfiguration = ChatPanelAPIConfiguration.shared) -> RequestBuilder<RuntimeActionResult> {
        var localVariablePath = "/v1/runtime/services/{id}"
        let idPreEscape = "\(id.rawValue)"
        let idPostEscape = idPreEscape.addingPercentEncoding(withAllowedCharacters: .urlPathAllowed) ?? ""
        localVariablePath = localVariablePath.replacingOccurrences(of: "{id}", with: idPostEscape, options: .literal, range: nil)
        let localVariableURLString = apiConfiguration.basePath + localVariablePath
        let localVariableParameters = JSONEncodingHelper.encodingParameters(forEncodableObject: runtimeServiceRequest, codableHelper: apiConfiguration.codableHelper)

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: (any Sendable)?] = [
            "Content-Type": "application/json",
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<RuntimeActionResult>.Type = apiConfiguration.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "POST", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true, apiConfiguration: apiConfiguration)
    }

    /**
     The runtime — the process sandbox, what is running now, the container engine, the services.
     
     - parameter apiConfiguration: The configuration for the http request.
     - returns: RuntimeDocument
     */
    open class func runtimeStatus(apiConfiguration: ChatPanelAPIConfiguration = ChatPanelAPIConfiguration.shared) async throws(ErrorResponse) -> RuntimeDocument {
        return try await runtimeStatusWithRequestBuilder(apiConfiguration: apiConfiguration).execute().body
    }

    /**
     The runtime — the process sandbox, what is running now, the container engine, the services.
     - GET /v1/runtime
     - One document for Settings › Runtime. `sandbox` is the bridge's own (`/health.sandbox`: enabled, mode `open` | `allowlist` | `none`, the runtime found, the reason when none, the global `extras`, whether a process can get a session of its own, and `refused` — the last hosts any process was refused, names only). `processes` lists every local MCP server running for the user with its sandbox record and the hosts it was refused, and the warm agent processes. `engines` says which container engine exists (`podman` first, `docker`) and whether it can run a container now; when none does, `install` carries the command for this platform — shown to the person, never run by the gateway. `services` is the catalogue: `searxng` with its state (`no-engine` | `engine-stopped` | `absent` | `stopped` | `running`), its loopback URL and whether it answers. 
     - Bearer Token:
       - type: http
       - name: gatewayToken
     - parameter apiConfiguration: The configuration for the http request.
     - returns: RequestBuilder<RuntimeDocument> 
     */
    open class func runtimeStatusWithRequestBuilder(apiConfiguration: ChatPanelAPIConfiguration = ChatPanelAPIConfiguration.shared) -> RequestBuilder<RuntimeDocument> {
        let localVariablePath = "/v1/runtime"
        let localVariableURLString = apiConfiguration.basePath + localVariablePath
        let localVariableParameters: [String: any Sendable]? = nil

        let localVariableUrlComponents = URLComponents(string: localVariableURLString)

        let localVariableNillableHeaders: [String: (any Sendable)?] = [
            :
        ]

        let localVariableHeaderParameters = APIHelper.rejectNilHeaders(localVariableNillableHeaders)

        let localVariableRequestBuilder: RequestBuilder<RuntimeDocument>.Type = apiConfiguration.requestBuilderFactory.getBuilder()

        return localVariableRequestBuilder.init(method: "GET", URLString: (localVariableUrlComponents?.string ?? localVariableURLString), parameters: localVariableParameters, headers: localVariableHeaderParameters, requiresAuthentication: true, apiConfiguration: apiConfiguration)
    }
}
